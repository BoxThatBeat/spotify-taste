---
phase: 01-oauth-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .env.example
  - .gitignore
  - server.js
autonomous: true

must_haves:
  truths:
    - "Node.js project is initialized with all required dependencies"
    - "Environment variables are configured for Spotify OAuth credentials"
    - "Express server can start without errors"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
      contains: "express"
    - path: ".env.example"
      provides: "Environment variable template"
      contains: "SPOTIFY_CLIENT_ID"
    - path: "server.js"
      provides: "Express server entry point"
      min_lines: 20
    - path: ".gitignore"
      provides: "Git ignore patterns"
      contains: "node_modules"
  key_links:
    - from: "server.js"
      to: "express"
      via: "require statement"
      pattern: "require\\(['\"]express['\"]\\)"
    - from: "server.js"
      to: ".env"
      via: "dotenv configuration"
      pattern: "dotenv\\.config"
---

<objective>
Initialize Node.js project with Express server foundation and environment configuration.

Purpose: Establish the technical foundation for OAuth implementation with all dependencies installed and basic server structure in place.

Output: Working Express server that starts successfully with environment variables loaded.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-oauth-foundation/01-CONTEXT.md

## Tech Stack (from research)
- Node.js 20.x LTS + Express 5.x
- spotify-web-api-node 5.x
- express-session 1.18.x
- dotenv 16.x

## Project Structure
This is a greenfield project. No existing code. Create standard Node.js/Express structure.
</context>

<tasks>

<task type="auto">
  <name>Initialize Node.js project with dependencies</name>
  <files>package.json</files>
  <action>
Create package.json with:
- name: "spotify-taste-match"
- version: "1.0.0"
- type: "commonjs" (spotify-web-api-node has CommonJS issues with ESM)
- main: "server.js"
- scripts:
  - "start": "node server.js"
  - "dev": "node --watch server.js" (Node 20+ built-in watch mode)
- dependencies:
  - express: "^5.0.0"
  - spotify-web-api-node: "^5.0.2"
  - express-session: "^1.18.0"
  - dotenv: "^16.4.5"
- engines:
  - node: ">=20.0.0"

Install dependencies: npm install
  </action>
  <verify>npm list shows all dependencies installed; no vulnerabilities warnings</verify>
  <done>package.json exists with correct dependencies; node_modules directory created</done>
</task>

<task type="auto">
  <name>Configure environment variables and .gitignore</name>
  <files>.env.example, .gitignore</files>
  <action>
Create .env.example with template:
```
# Spotify OAuth Credentials
# Get these from: https://developer.spotify.com/dashboard
SPOTIFY_CLIENT_ID=your_client_id_here
SPOTIFY_CLIENT_SECRET=your_client_secret_here
SPOTIFY_REDIRECT_URI=http://localhost:3000/callback

# Session Configuration
SESSION_SECRET=your_random_session_secret_here

# Server Configuration
PORT=3000
NODE_ENV=development
```

Create .gitignore with:
```
node_modules/
.env
.DS_Store
*.log
npm-debug.log*
.vscode/
.idea/
```

Do NOT create .env file (user must create manually with real credentials).
  </action>
  <verify>ls shows .env.example and .gitignore exist; .env is NOT committed</verify>
  <done>.env.example provides clear template for required environment variables; .gitignore prevents committing secrets</done>
</task>

<task type="auto">
  <name>Create Express server entry point</name>
  <files>server.js</files>
  <action>
Create server.js with:
- Require dotenv and configure at top (before other imports)
- Require express
- Create Express app instance
- Add basic middleware:
  - express.json()
  - express.urlencoded({ extended: true })
- Add health check route: GET / returns simple HTML "Spotify Taste Match - Server Running"
- Start server on PORT from .env (default 3000)
- Console log: "Server running on http://localhost:{PORT}"

Keep this minimal - no session management, no OAuth routes yet. Just prove server starts.
  </action>
  <verify>node server.js starts without errors (even without .env, should use defaults); curl http://localhost:3000 returns HTML response</verify>
  <done>Express server starts successfully and responds to HTTP requests on configured port</done>
</task>

</tasks>

<verification>
Run these checks:
1. npm list shows all dependencies installed
2. .env.example exists with all required variables documented
3. node server.js starts server without crashes
4. Server responds to HTTP requests on localhost:3000
5. .gitignore includes node_modules and .env
</verification>

<success_criteria>
- Node.js project initialized with package.json
- All dependencies (express, spotify-web-api-node, express-session, dotenv) installed
- .env.example documents all required environment variables
- Express server starts and responds to requests
- .gitignore prevents committing secrets
</success_criteria>

<output>
After completion, create `.planning/phases/01-oauth-foundation/01-01-SUMMARY.md`
</output>

---
phase: 04-visual-results
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [public/index.html, public/app.js, public/styles.css]
autonomous: false

must_haves:
  truths:
    - "User sees grid of shared artists with images and names after comparison"
    - "User sees grid of shared tracks with album covers and track details after comparison"
    - "User sees overall match percentage prominently displayed"
    - "User sees match breakdown (artists % and tracks %) when hovering or expanding"
    - "When no shared items exist, user sees encouraging empty state message"
    - "Layout works correctly on mobile devices (responsive grids, readable text)"
  
  artifacts:
    - path: "public/index.html"
      provides: "Results section HTML structure with containers for match percentage and grids"
      min_lines: 180
      contains: "results-section"
    
    - path: "public/app.js"
      provides: "Grid rendering logic that populates results from API response"
      min_lines: 120
      exports: []
      contains: "renderResults"
    
    - path: "public/styles.css"
      provides: "Responsive grid styles with desktop/mobile breakpoints"
      min_lines: 250
      contains: ".results-grid"
  
  key_links:
    - from: "public/app.js"
      to: "POST /api/compare response data"
      via: "renderResults function consuming sharedArtists and sharedTracks"
      pattern: "sharedArtists.*sharedTracks"
    
    - from: "public/app.js"
      to: "public/index.html results section"
      via: "DOM manipulation populating grid containers"
      pattern: "getElementById.*results"
    
    - from: "public/styles.css"
      to: "public/index.html grid elements"
      via: "CSS classes applied to grid items"
      pattern: "\\.grid-item|\\.results-grid"
---

<objective>
Replace alert-based results with visual grid display of shared artists and tracks, showing match percentage and responsive layout that works on mobile and desktop.

Purpose: Deliver the core visual experience - users see their shared music taste through engaging grids of images (artist photos and album covers).

Output: Polished results page with responsive grids, match percentage hero display, empty state handling, and mobile-optimized layout.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-visual-results/04-CONTEXT.md
@.planning/phases/03-comparison-engine/03-02-SUMMARY.md
@public/index.html
@public/app.js
@public/styles.css
@routes/comparison.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create results section HTML structure</name>
  <files>public/index.html</files>
  <action>
Add results section HTML structure below the comparison section (after the error div, before closing container div).

Structure to add:
1. **Results container** (id="results-section", initially hidden with style="display: none;")
2. **Match percentage hero section** with:
   - Large overall percentage display (id="overall-match")
   - Breakdown container (id="breakdown", shows artists % and tracks % on hover/click)
3. **Two-column grid container** (class="grids-container") containing:
   - Artists grid section: heading "Shared Artists" + grid container (id="artists-grid")
   - Tracks grid section: heading "Shared Tracks" + grid container (id="tracks-grid")
4. **Empty state containers** for zero matches (id="artists-empty" and id="tracks-empty", initially hidden)

Follow existing HTML patterns from index.html (semantic structure, consistent id naming).

DO NOT add inline styles beyond display:none for initial hidden state - styling comes in Task 3.
DO NOT modify existing comparison section or auth flow HTML.
  </action>
  <verify>
grep -E "results-section|overall-match|artists-grid|tracks-grid" public/index.html confirms structure exists.
HTML validates (no unclosed tags, proper nesting).
  </verify>
  <done>
Results section HTML structure exists in index.html with containers for match percentage, artist grid, track grid, and empty states.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement grid rendering logic in JavaScript</name>
  <files>public/app.js</files>
  <action>
Replace the alert() call in the compare button handler (line 44) with visual grid rendering.

Implementation:
1. **Create renderResults(data) function** that:
   - Extracts sharedArtists, sharedTracks, matchPercentage, breakdown, counts from API response
   - Shows results-section (display: block)
   - Hides comparison-section (shift focus to results)
   - Populates overall match percentage in #overall-match
   - Populates breakdown (artists % and tracks %) in #breakdown
   - Calls renderArtistsGrid() and renderTracksGrid()

2. **Create renderArtistsGrid(artists) function** that:
   - Clears #artists-grid innerHTML
   - If artists.length === 0: show #artists-empty, hide #artists-grid
   - Else: iterate over artists array and create grid items:
     - Each item: div with artist image (images[0].url or fallback), artist name below
     - Use images[1] if available (medium size, ~300px), fallback to images[0] or placeholder
     - Append to #artists-grid

3. **Create renderTracksGrid(tracks) function** that:
   - Clears #tracks-grid innerHTML
   - If tracks.length === 0: show #tracks-empty, hide #tracks-grid
   - Else: iterate over tracks array and create grid items:
     - Each item: div with album cover (track.album.images[1].url or fallback), track title, album name below
     - Show both track.name and track.artists[0].name
     - Append to #tracks-grid

4. **Handle missing images gracefully**:
   - If images array is empty or undefined, use placeholder: data URI or solid color background
   - Don't crash on missing data - defensively check arrays before accessing

5. **Replace alert() on line 44** with: renderResults(data)

6. **Add "See Results" / "Back to Comparison" toggle** (optional but good UX):
   - Add button to return from results view to comparison section
   - Or just refresh page to start over

Follow existing JS patterns: vanilla JS with getElementById, no frameworks.
Use const/let (not var), arrow functions, async/await style.
Keep error handling for retry button (lines 57-62) intact.
  </action>
  <verify>
grep "renderResults\|renderArtistsGrid\|renderTracksGrid" public/app.js confirms functions exist.
grep "alert" public/app.js should NOT find alert() in comparison flow (replaced).
Node syntax check: node --check public/app.js passes without errors.
  </verify>
  <done>
Grid rendering functions exist in app.js, alert() replaced with renderResults(), empty state logic handles zero matches, image fallbacks prevent crashes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add responsive grid styles with desktop/mobile layouts</name>
  <files>public/styles.css</files>
  <action>
Add CSS for results section, grids, and responsive behavior at end of styles.css.

Styles to add:

1. **Results section container** (.results-section):
   - Full width, centered content
   - Padding and margin consistent with existing .container
   - Background color matches body or subtle contrast

2. **Match percentage hero** (#overall-match, #breakdown):
   - Large font size for overall percentage (48px+)
   - Prominent placement at top
   - Breakdown text smaller (16-18px), positioned below or on hover
   - Color: Spotify green (#1DB954) for high match (70%+), neutral for medium (40-69%), encouraging tone for low (<40%)
   - Hover effect on breakdown to reveal details (transition, opacity change)

3. **Grids container** (.grids-container):
   - Desktop: side-by-side layout (display: flex, two columns)
   - Mobile: vertical stack (flex-direction: column)
   - Gap between grids (20-30px)

4. **Individual grid styling** (.results-grid for #artists-grid and #tracks-grid):
   - Display: CSS Grid (display: grid)
   - Desktop: 2-3 columns (grid-template-columns: repeat(auto-fit, minmax(150px, 1fr)))
   - Mobile: 1-2 columns (adjust minmax to 120px or similar)
   - Gap between items (15-20px)
   - Each grid has heading above ("Shared Artists", "Shared Tracks")

5. **Grid item styling** (.grid-item):
   - Image: square aspect ratio, rounded corners (border-radius: 8-12px)
   - Object-fit: cover (fill square without distortion)
   - Max width: 200px per item
   - Text below image: artist name or track title + album name
   - Text: 14-16px, truncate long text with ellipsis (text-overflow: ellipsis, white-space: nowrap, overflow: hidden)
   - Hover effect: subtle shadow or scale (transform: scale(1.05))

6. **Empty state styling** (.empty-state for #artists-empty, #tracks-empty):
   - Neutral/informational tone text
   - Center aligned, padding
   - Font size 16px, color #666
   - Display: none by default (JS shows when needed)

7. **Responsive breakpoint** (@media query):
   - Mobile breakpoint: max-width 768px
   - Changes: .grids-container flex-direction column, grid columns reduce to 1-2, font sizes slightly smaller

Follow existing CSS conventions: comments for sections, consistent spacing, mobile-first or desktop-first (match existing pattern in styles.css).
Use existing color variables if present (#1DB954 for Spotify green).
DO NOT modify existing button/auth styles (lines 1-115).
  </action>
  <verify>
grep -E "results-grid|grid-item|grids-container|@media" public/styles.css confirms styles exist.
CSS validates (no syntax errors, proper selectors).
Check responsive breakpoint exists with @media query for mobile.
  </verify>
  <done>
Responsive grid styles exist in styles.css with desktop 2-3 column layout, mobile 1-2 column layout, match percentage hero styling, empty state styles, and hover effects on grid items.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Visual results page with responsive grids displaying shared artists (with images) and shared tracks (with album covers), match percentage hero display, and empty state handling.

Replaced Phase 3's alert-based results with polished grid layout.
  </what-built>
  
  <how-to-verify>
**Prerequisites:** Both User A and User B authorized via OAuth (Phase 1 complete).

**Test Flow:**
1. **Start development server:**
   ```bash
   node server.js
   ```
   Server should start on port 3000

2. **Open browser to http://localhost:3000**
   - If both users already authorized: see comparison section
   - If not: complete OAuth flow for both users (User A, then User B)

3. **Trigger comparison:**
   - Select time range (try "Last 6 Months")
   - Click "Compare Now" button
   - Wait for loading spinner (should take 2-5 seconds)

4. **Verify results display (CRITICAL - this is the entire phase):**
   
   **Match Percentage Hero:**
   - [ ] Overall match percentage displays prominently at top (large number, e.g., "42% Match")
   - [ ] Breakdown shows artists % and tracks % (either always visible or on hover)
   - [ ] Match percentage uses appropriate tone/color (high = celebratory, low = encouraging)
   
   **Artist Grid:**
   - [ ] Grid of shared artists displays with artist images
   - [ ] Artist name appears below each image
   - [ ] Images are square with rounded corners
   - [ ] Desktop shows 2-3 artists per row
   - [ ] Grid shows ALL matched artists (scroll to see if many)
   
   **Track Grid:**
   - [ ] Grid of shared tracks displays with album covers
   - [ ] Track title + album name appears below each cover
   - [ ] Album covers are square with rounded corners
   - [ ] Desktop shows 2-3 tracks per row
   - [ ] Grid shows ALL matched tracks (scroll to see if many)
   
   **Layout:**
   - [ ] Desktop: artists grid on left, tracks grid on right (side-by-side)
   - [ ] Grids have consistent spacing and alignment
   - [ ] Text is readable (not too small, good contrast)
   
   **Empty State (test if you have non-overlapping accounts):**
   - [ ] When no shared artists: "No shared artists found" message displays
   - [ ] When no shared tracks: "No shared tracks found" message displays
   - [ ] Empty state is informational, not error-like
   - [ ] If one grid is empty and other has matches: both display correctly

5. **Test different time ranges:**
   - Try "Last 4 Weeks", "Last 6 Months", "All Time"
   - Each should re-fetch and display results for that time range
   - Results should update without page refresh

6. **Test responsive design (CRITICAL for VIS-08):**
   - Open browser dev tools
   - Toggle device toolbar (mobile view)
   - Verify:
     - [ ] Grids stack vertically (artists on top, tracks below)
     - [ ] Mobile shows 1-2 items per row (larger images, better focus)
     - [ ] Match percentage still readable
     - [ ] No horizontal scrolling
     - [ ] Text not cut off or overlapping
     - [ ] Images scale appropriately

7. **Test missing images (VIS-09):**
   - If any artists/tracks have missing images (rare with Spotify data)
   - Verify placeholder or fallback displays instead of broken image
   - No JavaScript errors in console

8. **Check for errors:**
   - Open browser console (F12)
   - No JavaScript errors during rendering
   - No broken image URLs (404s in Network tab)

**Expected Behavior:**
- Visual grid display replaces alert() completely
- Results are engaging and easy to scan
- Mobile experience is optimized (not just "works")
- Empty states are handled gracefully

**Common Issues to Check:**
- Images not loading: Check CORS, image URLs from Spotify API
- Layout broken on mobile: Verify @media query breakpoint
- Missing data crashes: Ensure defensive checks for empty arrays
- Grid items overlapping: Check CSS grid gap and sizing
  </how-to-verify>
  
  <resume-signal>
Type "approved" if results display works correctly on desktop and mobile, or describe specific issues to fix.
  </resume-signal>
</task>

</tasks>

<verification>
**Code verification:**
1. Results section HTML structure exists in public/index.html with semantic containers
2. renderResults(), renderArtistsGrid(), renderTracksGrid() functions exist in public/app.js
3. alert() call removed from comparison flow (replaced with renderResults)
4. Responsive grid styles exist in public/styles.css with @media query for mobile
5. Empty state handling logic exists for zero matches

**Functional verification (human checkpoint):**
1. Comparison triggers results display (no alert)
2. Match percentage displays prominently
3. Artist grid shows images and names
4. Track grid shows album covers and track details
5. Grids are responsive (side-by-side on desktop, stacked on mobile)
6. Empty states display when no shared items
7. Images handle missing data gracefully

**Requirements coverage:**
- VIS-01 ✓ Artist grid with images
- VIS-02 ✓ Track grid with album covers
- VIS-03 ✓ Artist names below images
- VIS-04 ✓ Track titles below covers
- VIS-05 ✓ Overall match percentage displayed
- VIS-06 ✓ Breakdown (artists % and tracks %)
- VIS-07 ✓ Empty state message
- VIS-08 ✓ Responsive mobile layout
- VIS-09 ✓ Missing image fallbacks
</verification>

<success_criteria>
**Must be TRUE:**
1. User triggers comparison and sees visual grid results (not alert)
2. Artist grid displays with images from Spotify API (sharedArtists array)
3. Track grid displays with album covers from Spotify API (sharedTracks array)
4. Match percentage and breakdown are prominently visible
5. Layout is responsive (desktop side-by-side, mobile stacked)
6. Empty states display when sharedArtists or sharedTracks arrays are empty
7. No JavaScript errors in console, no broken images
8. All VIS-01 through VIS-09 requirements met

**Deliverables:**
- public/index.html with results section structure
- public/app.js with grid rendering functions
- public/styles.css with responsive grid styles
- Phase 4 complete, v1 ready for end-to-end testing
</success_criteria>

<output>
After completion, create `.planning/phases/04-visual-results/04-01-SUMMARY.md`
</output>
